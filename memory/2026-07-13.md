## Site is broken — full rebuild needed

The site `sites/mom-and-pop-site/index.html` (deployed from `/tmp/mom-and-pop-site-temp/index.html`) has:
- **3 duplicate modals** with same id `waitlist-modal`
- **4 duplicate script blocks** with competing `useCase`, `openWaitlist`, `closeWaitlist`, `submitWaitlist` functions
- Broken `</button>` closing `</a>` tags (nav email link, pricing card links, CTA section)
- Form styling broken because of CSS conflicts between duplicates

**Fix:** Write the entire file from scratch. Clean HTML. One modal. One set of scripts. All CTA/pricing buttons use `onclick="openWaitlist()"` (not `<a>` tags).

## Dashboard fix needed

Dashboard summary card shows cumulative total `141` instead of showing the **leading label with /200**. Should show something like `devops: 141/200` as the big number, plus a separate "141 total traces" line below.

Per-label progress bars should show `/200` (they do this already in the dashboard, just the summary card is wrong).

## Pending

- Formspree endpoint for waitlist (action=`https://formspree.io/f/TBD`) — Charles needs to create form and provide ID
- OpenRouter API key in `.env` for `classify-traces.py` to work on exported traces
