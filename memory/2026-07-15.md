# 2026-07-15

## Pinned: Protocol & Architecture Research
Charles wants the best protocol/architecture (event-driven?) for the FamCloud/The Desk platform — distributed edge AI boxes communicating with a mothership.

## Repo Cleanup (Morning)
- Consolidated 18 strategy/hardware/pricing docs into 3 in `experiment/business/`
- Moved 22 stale docs, whisper outputs, old trace formats to `.archive/`
- Nuked `client_secret.json` from git history with git-filter-repo
- Deleted 14 whisper transcript files and old format traces
- Charles needs to: rotate the Google OAuth secret (was in git history for 4 days)

## Cron Job Fix
- Failed on step 2 — `classify-traces.py` couldn't get `OPENROUTER_API_KEY` (no `.env` file)
- Need Charles to provide the key for standalone scripts
- Updated verticals/capabilities in classify script to match what these traces actually are

## HuggingFace Setup
- Need Charles's HF token to create dataset repo and upload 66 traces
- Plan: HF dataset for traces, HF for model weights — same ecosystem
